{% extends 'base.html.twig' %}

{% block title %}Patient {% endblock %}

{% block body %}
  

    
{#  #}
{# <style>
div.input-group-append{
margin-right: 0px;
}
</style> #}
  
      {{ form_start(form) }}
   <div class="container">
    <div class="row">
               <div class="header">
                   
               </div>
               <div class="body">
                    <div class="input-group"  >
                    

                    <div>
                       <a href="{{ path('add_patient') }}"  class="btn btn-lg btn-info"><i class="material-icons">&#xE147;</i> <span>Add New Patient</span></a>
                   </div>
                     <div class="input-group-append" style="margin-left:50%;">
                   
                       {{ form_widget(form.FirstName, {'attr': {'class': 'input-group-append','placeholder': '   search  patient'}}) }}
                       {{ form_help(form.FirstName) }}
                       {{ form_errors(form.FirstName) }}
                      
                       
                       {{ form_widget(form.Search,{'attr': {'class': 'fa fa-search btn btn-info'}})}}
                     
                       
                       </div>
                       
                   
              </div>
               </div>
                   
   {{ form_end(form) }}

    <table class="table table-striped table-hover" class="col-sm-8">
     <thead>

                        <tr>
                             {# <th>
                                <span class="custom-checkbox">
                                    <input type="checkbox" id="selectAll">
                                    <label for="selectAll"></label>
                                </span>
                            </th>  #}
                            <th>#</th>
                            <th>FirstName</th>
                            <th>LastName</th>
                            <th>Cin</th>
                            <th>Gender</th>
                             <th>Tel</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                   <tbody>
                   {% for patient in patients %}
                      <tr>
                             {# <td>
                                <span class="custom-checkbox">
                                    <input type="checkbox" id="checkbox1" name="options[]" value="1">
                                    <label for="checkbox1"></label>
                                </span>
                            </td>  #}
                            <td>{{ patient.id }}</td>
                            <td>{{ patient.FirstName }}</td>
                            <td>{{ patient.LastName }}</td>
                            <td>{{ patient.Cin }}</td>
                            <td>{{ patient.Gender }}</td>
                            <td>{{ patient.Tel }}</td>
                            <td >
                             <a href="{{ path('show_patient', {'id': patient.id}) }}" class="glyphicon glyphicon-cog"><i style="font-size:24px" class="fa" title="Show">&#xf007;</i></a>
                                <a href="editPatient/{{patient.id}}" class="edit" ><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
                              <a href="deletePatient/{{patient.id}}" data-toggle="modal" data-target="#delete{{ patient.id}}"class="edit" ><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>
                              {# <a href="#editEmployeeModal" class="edit" data-toggle="modal"><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a> #}
                            </td>
                        </tr>

 {{ include('patient/_delete_modal.html.twig') }}
 


  {% endfor %}
  </tbody>                      
  </table>
    </div>
    </div>

 {% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Patient {% endblock %}

{% block body %}
  

    
 {# #}
{# <style>
div.input-group-append{
margin-right: 0px;
}
</style> #}
  
      {{ form_start(form) }}
   <div class="container">
    <div class="row">
               <div class="header">
                   
               </div>
               <div class="body">
                    <div class="input-group"  >
                    

                    <div>
                       <a href="{{ path('add_consultation') }}"  class="btn btn-lg btn-info"><i class="material-icons">&#xE147;</i> <span>Add consultation</span></a>
                   </div>
                     <div class="input-group-append" style="margin-left:50%;">
                   
                       {{ form_widget(form.Patient, {'attr': {'class': 'input-group-append','placeholder': '   search consultation'}}) }}
                       {{ form_help(form.Patient) }}
                       {{ form_errors(form.Patient) }}
                      
                       
                       {{ form_widget(form.Search,{'attr': {'class': 'fa fa-search btn btn-info'}})}}
                     
                       
                       </div>
                       
                   
              </div>
               </div>
                   
   {{ form_end(form) }}
   
    <table class="table table-striped table-hover" class="col-sm-8">
     <thead>

                        <tr>
                             {# <th>
                                <span class="custom-checkbox">
                                    <input type="checkbox" id="selectAll">
                                    <label for="selectAll"></label>
                                </span>
                            </th>  #}
                            <th>#</th>
                            <th>Doctor</th>
                            <th>Patient</th>
                            <th>Date</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                   <tbody>
                   {% for consultation in consultations %}
                      <tr>
                             {# <td>
                                <span class="custom-checkbox">
                                    <input type="checkbox" id="checkbox1" name="options[]" value="1">
                                    <label for="checkbox1"></label>
                                </span>
                            </td>  #}
                            <td>{{ consultation.id }}</td>
                            <td>{{ consultation.Doctor.FullName }}</td>
                            <td>{{ consultation.Patient.FullName }}</td>
                            <td>{{ consultation.Date|date('Y-m-d')  }}</td>
                            {# <td>{{ patient.Gender }}</td>
                            <td>{{ patient.Tel }}</td> #}
                            <td >
                             <a href="{{ path('show_consultation', {'id': consultation.id}) }}" class="glyphicon glyphicon-cog"><i style="font-size:24px" class="fa" title="Show">&#xf007;</i></a>
                                <a href="editConsultation/{{consultation.id}}" class="edit" ><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
                              <a href="deleteConsultation/{{consultation.id}}" data-toggle="modal" data-target="#delete{{ consultation.id}}"class="edit" ><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>
                              {# <a href="#editEmployeeModal" class="edit" data-toggle="modal"><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a> #}
                            </td>
                        </tr>

 {{ include('consultation/_delete_modal.html.twig') }}
 


  {% endfor %}
  </tbody>                      
  </table>
    </div>
    </div>

 {% endblock %}
{% extends 'base.html.twig' %}

{% block title %}Consultation{% endblock %}

{% block body %}
   




        <div class="container">
            <div class="row">
                <div class="col-12 bg-white ">
                {##}
                    <div class="d-flex flex-column align-items-center" <img class="photo" src="https://images.unsplash.com/photo-1541647376583-8934aaf3448a?ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&ixlib=rb-1.2.1&auto=format&fit=crop&w=634&q=80" alt="">

                        {# <p class="fw-bold h4 mt-3">{{ [patient.LastName|upper, ' ', patient.FirstName, ] | join }}</p> #}
                       
                        <div class="d-flex ">
                            <a href="{{ path('edit_consultation', {'id': consultation.id}) }}"  class="btn btn-primary follow me-2" >edit</a>
                     <a href="deleteConsultation/{{consultation.id}}"  class="btn btn-outline-primary message" data-toggle="modal" data-target="#delete{{ consultation.id}}" >Delete</a>

                        </div>
                    </div>
                </div>
                
        </div>
      
        <div class="container">
            <div class="row">
                <div class="col-12 bg-white ">
                   
                    <div class="d-flex align-items-center justify-content-between border-bottom">
                        <p class="py-2">Doctor</p>
                        <p class="py-2 text-muted">{{ consultation.Doctor.FullName}}</p>
                    </div>
                    <div class="d-flex align-items-center justify-content-between border-bottom">
                        <p class="py-2">Last Name</p>
                        <p class="py-2 text-muted">{{ consultation.Patient.FullName }}</p>
                    </div>
                    <div class="d-flex align-items-center justify-content-between border-bottom">
                        <p class="py-2">Cin</p>
                        <p class="py-2 text-muted">{{ consultation.Date|date('Y-m-d') }}</p>
                    </div>
                   
                </div>
               
            </div>
        </div>
   
 {{ include('consultation/_delete_modal.html.twig') }}
{% endblock %}